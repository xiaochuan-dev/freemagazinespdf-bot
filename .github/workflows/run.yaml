name: run

on:
  push:
  workflow_dispatch:

jobs:

  run:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - run: |
          curl -I https://freemagazinespdf.com
      
      